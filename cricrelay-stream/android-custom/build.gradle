// JitPack for RootEncoder (screen-capture RTMP). No evaluationDependsOn — breaks Gradle 8+.
allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}
