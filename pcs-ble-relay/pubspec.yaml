name: pcs_ble_relay
description: CricRelay PCS BLE relay — forwards Play Cricket Scorer data to CricRelay.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.4.0
  http: ^1.2.2
  shared_preferences: ^2.3.2
  permission_handler: ^11.3.1
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
