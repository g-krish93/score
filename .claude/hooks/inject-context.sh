#!/usr/bin/env bash
echo "Architecture file is at docs/ARCHITECTURE.md — read it before making any structural changes. Always update it after adding features, changing module structure, or adding dependencies."
